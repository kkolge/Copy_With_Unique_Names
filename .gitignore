# Python bytecode
*.pyc
__pycache__/

# Virtual environment
# Typically 'venv', '.venv', 'env' or 'build_env'
# Add any other names you might use for your virtual environments
venv/
.venv/
env/
build_env/
old/
logs/

# PyInstaller build artifacts
build/
dist/
settings.json

# Log files
*.log # Ignores files like log.log generated by the logger_setup

# User-specific configuration/data files (if they might appear in the project root during dev)
# The application usually saves config.json to a user's data directory,
# but if a temporary or default one exists in the project root, ignore it.
config.json
settings.json # Include if you ever use this name

# Operating System files
.DS_Store # macOS directory services files
Thumbs.db # Windows thumbnail cache files

# IDE specific files
.idea/          # PyCharm project files
.vscode/        # VS Code configuration files
*.iml           # IntelliJ IDEA module files
*.ipr
*.iws

# Python installer/distributor files
*.egg-info/
.installed.cfg
pip-log.txt

# Development related files
.coverage       # Coverage.py files
.cache/         # General cache directory
.pytest_cache/  # pytest cache
.mypy_cache/    # mypy static analysis cache
.nox/
.hypothesis/
htmlcov/
.tox/